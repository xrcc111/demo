/* package main

import (
	"fmt"
)

func main() {
	arrLength()
}
*/
/* func test() {
	var arr1 [2]int
	arr1[0] = 10
	arr1[1] = 20
	fmt.Printf("arr1: %v\n", arr1)
	arr1[0] = 100
	arr1[1] = 200
	fmt.Printf("arr1: %v\n", arr1)
}
*/

/* func arrLength() {
	var arr = [...]int{10, 1, 2, 3, 100}
	fmt.Printf("len(arr): %v\n", len(arr)) // len 获取数组的长度
	// 单纯for 循环
	for i := 0; i < len(arr); i++ {
		fmt.Printf("arr[%v]: %v\n", i, arr[i])
	}
	// for range
	for i, v := range arr {
		fmt.Printf("%v, %v\n", i, v)
	}
}
*/